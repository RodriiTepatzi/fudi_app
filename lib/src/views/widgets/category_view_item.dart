import 'package:flutter/material.dart';

class CategoryViewItem extends StatelessWidget {
  const CategoryViewItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}